[package]
name = "tagentacle-py-inferencemux"
version = "0.1.0"
type = "library"
description = "Agent SDK — composable agent building blocks"

[dependencies]
tagentacle-py-core = { git = "https://github.com/Tagentacle/tagentacle-py-core.git" }
